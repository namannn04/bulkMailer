/**
 * Process HTML content to ensure better compatibility with email clients
 */
export function processEmailHtml(html: string): string {
    // This function can be expanded with more email-specific processing
    // For now, we're just returning the HTML as is
    return html
  }
  